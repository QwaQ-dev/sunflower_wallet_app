import { View } from "lucide-react-native";

type StackingDaoCardProps = {
    walletName: string;
}

export default function BTCzCard({walletName}: StackingDaoCardProps ) {
    return(
        <View>
            
        </View>
    )
}