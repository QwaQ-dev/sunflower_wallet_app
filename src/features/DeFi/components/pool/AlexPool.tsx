import { View } from "lucide-react-native";

type AlexPoolCardProps = {
    walletName: string;
}

export default function AlexPoolCard({walletName}: AlexPoolCardProps ) {
    return(
        <View>
            
        </View>
    )
}